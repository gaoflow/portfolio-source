---
import ProjectsIndex from '../../components/ProjectsIndex.astro';
import { getCollection } from 'astro:content';

const projects = await getCollection('projects');
---

<ProjectsIndex projects={projects} locale="en" hasAlternate={false} />
